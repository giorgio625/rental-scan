# Rental Scan — 2026-08-18

New: 0 · Price changes: 0 · Relists: 0 · Near misses: 0 · Possible duplicates: 0 · Unresolvable addresses: 0

**This was an infrastructure outage, not a quiet market day.** Both harvesters failed before collecting anything:

- **`inbox-harvester`** — Gmail MCP access unavailable. Did not read the `rental-alerts` label at all.
- **`web-scout`** — WebFetch egress blocked network-wide. Did not reach any of the non-syndicating sources.
- **`dedupe-analyst`** ran against zero input from both, so it correctly reports zero new, zero price changes, zero relists. `ledger.json`'s live set is byte-identical to 2026-08-17: **43 live units**, only `meta.last_run` bumped to 2026-08-18.
- **`geocoder`** also hit a block — Nominatim returned 403 — and could not backfill the 10 pre-existing ledger units still missing `lat`/`lng`. It made no changes to the ledger (no partial or bad writes), so this is a stalled backfill, not new data loss.

No listing data was lost — `ledger.json` simply didn't get a chance to change today. But every source that normally feeds this scan returned zero today, which is a different failure mode than the usual per-source gap (compare 2026-08-17's report, where four of five sources reported and only Apartments.com was silent). Nothing below reflects today's market; it reflects yesterday's, carried forward untouched.

---

## Priority (70+)
None. (No new data collected today.)

## Worth a look (50–69)
None. (`shortlist.json` carries forward unchanged from 2026-08-17 — still nothing scoring 50+.)

## Near misses
None collected — not because nothing was close, but because nothing came in to evaluate. See 2026-08-17's report for the last real near-miss batch (16 listings, including the Southport hard-loft pair that missed only on availability).

---

## Price changes
None.

## Possible duplicates
None.

---

## Sources that returned zero

**All of them.** Every source this scan normally draws from — Gmail alerts (Zillow, Redfin, Apartments.com, Domu, Craigslist) via `inbox-harvester`, and the non-syndicating web sweep via `web-scout` — returned zero today because the collection tools themselves failed (Gmail MCP unavailable; WebFetch egress blocked network-wide), not because the sources had nothing to say. Treat today as a gap in coverage, not a signal about the market. `geocoder`'s Nominatim calls also 403'd, so the 10 ledger units still missing coordinates remain ungeocoded going into tomorrow.

If this repeats tomorrow, it's worth checking network/auth status directly rather than trusting another empty run.

---

## State files (rebuilt regardless of today's outage)

Per the standing rule, `shortlist.json`, `active.md`, and both `docs/*.html` dashboards are rebuilt every run — including this one — so a listing that died would still drop off today. Nothing died and nothing was added: `shortlist.json` stays an empty array, `active.md` is unchanged apart from its date stamp, and both dashboards were regenerated with an empty listings array and a header note flagging today's outage.
