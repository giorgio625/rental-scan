# Rental Scan — 2026-08-20

New: 1 · Price changes: 0 · Relists: 0 · Near misses: 1 · Possible duplicates: 0 · Unresolvable addresses: 2

Quietest run since the streak started. 42 previously-known listings came back unchanged (suppressed per §7). The one new find has almost no data to work with — see below.

---

## Priority (70+)
None.

## Worth a look (50–69)
None.

---

## Near misses

**`1407-n-milwaukee-UNKNOWN`** — 1407 N Milwaukee Avenue, Wicker Park core (geocoded 41.9075, -87.6727 — well inside bounds). No unit number, no beds/baths, no rent published, no availability date. `cost_assumptions` from `dedupe.py`: *"no base rent published."*

Fails **style** only, and only by mechanical default: `loft_signals` is `null` (not even an empty array — nothing was extracted), so under §4a there's nothing to count toward the 3-of-5 hard-loft or 3-of-4 soft-loft bar, and the test resolves to `not_loft`. Rent, beds, and availability are all unknown rather than confirmed failing, so this isn't a listing that's "one criterion away" in the usual sense (the classic near-miss case, e.g. rent $50 over ceiling) — it's a listing with almost nothing to evaluate. `cost_assumptions` suggests this is a building/floorplan-level MRED page (the same category §6a says to leave as-is rather than force into a unit-level record), not a live unit listing yet. Worth a look only if the source page gets a real unit and price attached in a later run.

**Not on the HTML dashboards today** — the card template renders price unconditionally from `rent_gross`, and inventing a number to avoid a null isn't an option here. Full detail stays in this report; it'll join the dashboard if a later run gives it a real rent.

---

## Price changes
None.

---

## Possible duplicates
None today — re-confirmed against `raw/classified-2026-08-20.json`'s `possible_duplicates` array (empty) and `counts.possible_duplicates` (0).

---

## Unresolvable addresses (2) — needs a human look

Both Craigslist, both claiming Lincoln Park, neither with a parseable street address — `dedupe.py` can't build a canonical key, so these aren't bounds-checked, scored, geocoded, or tracked in `ledger.json`.

- $2,600 1BR — [listing](https://www.craigslist.org/view/d/chicago-live-in-the-heart-of-lincoln/svR8NKK2gMcZU5w25kyqGb)
- $2,110 1BR — [listing](https://www.craigslist.org/view/d/chicago-live-on-the-park-luxurious/w6knhwxFYpHK6JtfJFLhNu)

---

## Sources

Seen today: craigslist, redfin, web, zillow.

**Returned zero: Apartments.com — fifth consecutive run** (2026-08-13, 08-17, 08-18, 08-19, 08-20). This is no longer a blip — worth checking the saved-search alert subscription directly.

**Returned zero: Domu — second consecutive run** (08-19, 08-20). Flagged as an unusual one-off yesterday after being present through 08-18; two in a row makes it worth watching rather than dismissing as noise, though not yet at the same level of concern as Apartments.com.
